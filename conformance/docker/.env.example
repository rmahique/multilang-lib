# Copy to .env (in this directory) to override the database versions
# used by docker-compose.yml, or export these same names before running
# ../run-live-db-tests.sh directly.
POSTGRES_VERSION=16
MYSQL_VERSION=8.0
POSTGRES_PORT=5432
MYSQL_PORT=3306
